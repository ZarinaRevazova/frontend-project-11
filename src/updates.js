/* const addTrackedFeed = (url) => {
  state.trackedFeeds.push({
    url,
    lastCheckTime: Date.now(),
    lastPostCount: 0, // будем сохранять число постов при последней проверке
  });
};

export default addTrackedFeed;
*/
